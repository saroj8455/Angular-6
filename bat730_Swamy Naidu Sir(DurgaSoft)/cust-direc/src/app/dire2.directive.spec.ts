import { Dire2Directive } from './dire2.directive';

describe('Dire2Directive', () => {
  it('should create an instance', () => {
    const directive = new Dire2Directive();
    expect(directive).toBeTruthy();
  });
});
